# Local / notebook deps — keep HF stack aligned with requirements-backend.txt.
# torch pin differs: backend uses 2.9.1 CPU (Pi); this env keeps a newer CUDA torch.

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
jinja2==3.1.3
python-multipart==0.0.6

# Configuration
python-dotenv==1.0.0

# Database
psycopg2-binary==2.9.11

# HuggingFace Inference API
numpy==1.26.4; python_version < "3.13"
numpy==2.3.5; python_version >= "3.13"
tiktoken==0.12.0

# HuggingFace stack - pinned exactly (same as requirements-backend.txt)
huggingface-hub==1.29.0
transformers==5.16.1
tokenizers==0.23.1
sentence-transformers==6.0.1

# Utilities
python-dateutil==2.8.2
requests==2.32.5
tqdm==4.67.1

# Notebook
ipykernel>=6.29.0

# PyTorch: do not pin here for local CUDA installs.
# Backend/Pi: torch==2.9.1 from the PyTorch CPU index (see requirements-backend.txt).
